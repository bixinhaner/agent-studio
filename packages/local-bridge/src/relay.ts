import { randomUUID } from 'node:crypto';
import type { BridgeRequest } from './protocol.js';
export type BridgeDevice={id:string;userId:string;name:string;connected:boolean;lastSeenAt?:string;roots:string[]};
export class BridgeRelay { private devices=new Map<string,BridgeDevice>(); register(userId:string,name:string,roots:string[]){const d={id:randomUUID(),userId,name,connected:true,lastSeenAt:new Date().toISOString(),roots:[...roots]};this.devices.set(d.id,d);return d;} list(userId:string){return [...this.devices.values()].filter(d=>d.userId===userId);} authorize(deviceId:string,userId:string,sessionId:string,op:BridgeRequest['op'],path:string):BridgeRequest{const d=this.devices.get(deviceId);if(!d||d.userId!==userId||!d.connected)throw new Error('BRIDGE_DEVICE_UNAVAILABLE');return {id:randomUUID(),scope:{userId,deviceId,sessionId,roots:d.roots},op,path};} disconnect(deviceId:string,userId:string){const d=this.devices.get(deviceId);if(!d||d.userId!==userId)throw new Error('BRIDGE_DEVICE_UNAVAILABLE');d.connected=false;}}
