import test from 'node:test';import assert from 'node:assert/strict';import {BridgeRelay} from './relay.js';test('binds devices to user',()=>{const r=new BridgeRelay();const d=r.register('u','Mac',['/tmp']);assert.equal(r.list('u').length,1);assert.throws(()=>r.authorize(d.id,'other','s','read','/tmp'));r.disconnect(d.id,'u');assert.throws(()=>r.authorize(d.id,'u','s','read','/tmp'));});
