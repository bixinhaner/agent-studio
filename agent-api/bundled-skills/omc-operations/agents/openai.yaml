interface:
  display_name: "OMC Operations"
  short_description: "Help operations users inspect, diagnose, and safely change their managed network"
  default_prompt: "Use $omc-operations to complete the operational request. Reuse the active handbook and proven operations, prefer its common-operation fast paths, and stop when evidence is sufficient. Answer in clear operational language with the conclusion, impact, and useful next action; hide APIs, tools, handbook internals, and execution details unless the user explicitly asks for diagnostics."
